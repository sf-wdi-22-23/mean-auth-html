html
  body
    table.table
      tr
        th.h4.bold
          | Name
        th.h4.bold
          | Email
        th.h4.bold
          | Opt-in
        th.h4.bold
          | Notes
      tr(ng-repeat='(index, post) in posts')
        td
          a.h3(ng-href='/records/{{post._id}}/edit') {{post.name}}
        td {{post.email}}
        td {{post.optin}}
        td {{post.notes}}
