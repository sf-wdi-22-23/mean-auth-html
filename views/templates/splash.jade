html
  body
    .jumbotron
      .container.text-center
        h1 Vivifi
        .lead Smarter Sexual Assault Case Management
        .btn-toolbar(ng-hide='currentUser')
          a.btn.btn-lg.btn-info(data-toggle='modal', data-target='#login-modal') Get Started
        .btn-toolbar(ng-show='currentUser')
          a.btn.btn-lg.btn-info(href='/create') + Create new record
        .btn-toolbar(ng-show='currentUser && currentUser.admin')
          a.btn.btn-lg.btn-info(href='/records') View all the records
    .container
      .row
        .col-sm-4
          h3 Improve Data Quality
        .col-sm-4
          h3 Efficient Communication with Victims
        .col-sm-4
          h3 Accelerate Case Management
